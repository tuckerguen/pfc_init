PFC_Init(left_path, right_path, template_path)
    NeedleImage left_image
    NeedleImage right_image
    NeedleTemplate template
    ComputeNeedlePose()
    ScorePoseEstimation(Pose)

NeedleImage
    Constructor(image_path) //Loads and preprocesses image based on input string

NeedleTemplate //Could just be a version of NeedleImage?
    Constructor(template_path)
    Rotate

Match
    PrintMatchInformation / PrintResultsForImage
    DrawOnImage(Mat)

Pose
    location
    orientation
    getRPYOrientation()
    getQuaternionOrientation()
